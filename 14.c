#include<stdio.h>
#include<conio.h>
 int v=15,source,m[15][15],visited[15],min
 void DFS(int m[15][15],int v,int source)
 {
     int i,visited[source]=1;
     min=m[source][0];
     for(i=0;i<v;i++)
     {
         if(m[source][i]<min)
         {
             min=
         }
         if (m[source][i]!=0&&visited[i]!=1&&)
     }
 }
